<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zerohunger.geneiryodan.feedapp.views">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewActionFeedBikeActivity" />
        <activity android:name=".NewActionFeedActActivity" />
        <activity android:name=".ApproverBenevActivity" />
        <activity
            android:name=".FilActualiteActivity"
            android:label="@string/title_activity_fil_actualite"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpResult" />
        <activity android:name=".ChoiseActionActivity"></activity>
    </application>

</manifest>